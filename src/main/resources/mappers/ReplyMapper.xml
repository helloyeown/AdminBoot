<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.zerock.board.mappers.ReplyMapper">

<select id="getList" resultType="ReplyDTO">
    select if(rno=gno, 0, 1) as step, rno, bno, reply, replyer, replyData, gno
    from tbl_reply
    where bno=#{bno} and rno>0
    order by gno asc, rno asc
    limit #{pr.skip}, #{pr.size}
</select>

<select id="getRnoCnt" resultType="int">
    select count(rno) from tbl_reply where bno=#{bno}
</select>

</mapper>